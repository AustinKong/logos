1. Installation guide/new developer setup guide. Currently its abit complex with codegen
2. Setup precommit hooks that does validation and generates new openapi.json so its committed up to date
3. Investigate how the Fastapi model/schema convention will affect codegen, especially wrt affecting query params. Does query param need special naming convention etc.
4. Setup agent skills for commit conventions etc.
